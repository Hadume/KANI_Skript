command /reset_area:
    permission: Admin.Permission
    trigger:
        if player is "H_Y_K":
            initArea("%uuid of player%")
        else:
            send "<bold><light red>このコマンドは、はづめ様しか使えないよ！" to player
