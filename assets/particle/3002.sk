on load:
    if {objects::registered::*} do not contain 3002:
        add 3002 to {objects::registered::*}

every ticks:
    if {objects::enabled::*} contain 3002:
        draw 1 end rod particle with offset vector(2, 2, 4) with extra value of 0 at location(-647.5, 100, 189.5)
