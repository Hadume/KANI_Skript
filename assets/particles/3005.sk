on load:
    particle_register({@id})


every tick:
    exit if ({particles::enabled::*} does not contain {@id})

    force draw 1 firework particle with offset vector(5.0, 5.0, 4.0) with extra value of 0.01 at location(-494.5, 80.5, 200.5)


options:
    id: 3005
